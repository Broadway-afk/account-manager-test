# Тестовое задание для SaaSoft

Привет!  
Сделал это тестовое задание со всей душой и любовью ❤️  
Старался максимально соответствовать ТЗ, при этом добавить немного качества и предусмотреть будущие улучшения.

## Стек

- Vue 3 (Composition API + `<script setup>`)
- TypeScript
- Pinia + pinia-plugin-persistedstate (сохранение в localStorage)
- Naive UI (компоненты + тёмная тема)

## Основные реализованные требования

- Добавление новой пустой записи по кнопке "+"
- Удаление записи (с подтверждением через confirm)
- Динамическое отображение/скрытие поля "Пароль" в зависимости от типа
- Валидация обязательных полей по blur / change
- Автоматическое сохранение при успешной валидации
- Поле "Метки" → массив объектов `{ text: string }` с разделителем `;`
- Данные сохраняются между перезагрузками страницы
- Полная типизация (Account vs AccountFormDTO, маппинг через toAccount)

## Технические решения и компромиссы
- **Переход на uid вместо индекса**  
В процессе разработки понял, что работа по индексу массива - это мина замедленного действия (особенно если в теоретическом будущем добавлять сортировку, фильтр или drag-n-drop). Поэтому перешёл на работу по `uid` в store и в emit.
- **Подтверждение удаления**  
  Добавил модалку перед удалением - считаю, что это важно даже в простом тестовом.
- **Auto-save по blur**  
  Реализовал именно так, как написано в ТЗ - "по окончанию ввода значений".  
  В реальном проекте скорее всего добавил бы кнопку "Сохранить" + dirty-check или хотя-бы debounce, но здесь строго следовал формулировке.
- **Такие себе коммиты**  
  Немного увлекся и коммитил по настроению 😅 
  На реальных проектах стараюсь придерживаться принципов atomic commits
- **Валидация "своими руками"**  
  Честно говоря, от функции validate() мне грустно, но так бывает с валидацией.
  По-хорошему, тут стоило бы использовать обертку формы от NaiveUI со встроенной валидацией или библиотеку
  вроде yup (мне она очень нравится), но я решил написать самостоятельно, ведь в стеке о таком ни слова.

## Project Setup

```sh
yarn
```

### Compile and Hot-Reload for Development

```sh
yarn dev
```

### Type-Check, Compile and Minify for Production

```sh
yarn build
```

### Lint with [ESLint](https://eslint.org/)

```sh
yarn lint
```
